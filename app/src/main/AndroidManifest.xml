<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mywallet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyWallet">
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UpdateTrosak"
            android:label="Izmjena troška" />
        <activity
            android:name=".UpdatePrihod"
            android:label="Izmjena prihoda" />
        <activity
            android:name=".UnosZapisa"
            android:label="Unos zapisa" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/button_bg_round" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyWallet.NoActionBar">

        </activity>
    </application>

</manifest>